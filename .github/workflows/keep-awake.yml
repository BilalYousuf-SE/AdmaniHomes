name: Keep Backend Awake
   on:
     schedule:
       - cron: '*/10 4-19 * * *'
   jobs:
     ping:
       runs-on: ubuntu-latest
       steps:
         - name: Ping backend health check
           run: curl -f -s https://admanihomes.onrender.com/actuator/health
